export enum FieldState {
    VALID,
    VALIDATING,
    INVALID,
    WARNING,
  }

export enum FieldType {
  TEXT,
  EMAIL,
  PASSWORD,
  SELECT,
  CHECKBOX,
  TEXTAREA,
}

export enum FormResultState {
  SUCCESS,
  FAILURE,
}
